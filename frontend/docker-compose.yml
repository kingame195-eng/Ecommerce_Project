version: "3.8"
services:
  react-app:
    build: .
    ports:
      - "4173:4173"
    volumes:
      - .:/app
      - /app/node_modules # Prevents local node_modules from overwriting container modules
    environment:
      - CHOKIDAR_USEPOLLING=true # Often required for HMR inside Docker containers
